"use strict";
var casting;
(function (casting) {
    let idade = 25.32345;
    idade.toFixed(2);
    idade.length;
    idade.forEach((x) => {
        console.log(x);
    });
    let nome = 35;
})(casting || (casting = {}));
